$ ->

    #---------------------------------------------------------------------------
    # manga info page
    #---------------------------------------------------------------------------

    if $('#template-manga-info').length

        $('h2').click ->
            id = $(this).attr('id')
            $("[data-section=#{id}]").toggleClass('active')

